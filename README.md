# CakePHP Development Docker Image

PHP docker image for CakePHP development.

Images are build with tags `8.1`, `8.2`, `8.3` which represent the PHP version. You can pull the image from:

`ghcr.io/gregor-j/php-cake-dev:${PHP_VERSION}`
